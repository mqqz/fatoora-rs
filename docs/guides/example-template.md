# Guide Template

Use this page as a template for new guides with multi-language examples.

## Goal
Describe the goal in one or two sentences.

## Example

=== "Rust"
    ```rust
    // Rust example
    ```

=== "Python"
    ```python
    # Python example
    ```

=== "C/C++"
    ```c
    /* C example */
    ```

## Notes
- Call out required inputs, optional inputs, and output shape (e.g., string, handle, structured type).
- Include at least one edge case or common error to help users troubleshoot quickly.
